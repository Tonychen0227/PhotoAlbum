# Generated by Containerization Assist using AI
# Overrides for the 'dev' environment — applied on top of values.yaml
replicaCount: 1

image:
  repository: crpetclinicdev.azurecr.io/photoalbum
  tag: "dev"

env:
  ASPNETCORE_ENVIRONMENT: "Development"

ingress:
  enabled: true
  hosts:
    - host: photoalbum-dev.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: photoalbum-dev-tls
      hosts:
        - photoalbum-dev.example.com

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "250m"
    memory: "256Mi"

autoscaling:
  enabled: false
