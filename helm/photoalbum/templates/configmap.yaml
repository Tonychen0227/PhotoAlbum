# Generated by Containerization Assist using AI
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "photoalbum.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "photoalbum.labels" . | nindent 4 }}
data:
  # ASP.NET Core runtime settings
  ASPNETCORE_ENVIRONMENT: {{ .Values.env.ASPNETCORE_ENVIRONMENT | quote }}
  ASPNETCORE_URLS: {{ .Values.env.ASPNETCORE_URLS | quote }}
  # Application feature flags (non-sensitive)
  IsTestEnvironment: {{ .Values.config.isTestEnvironment | quote }}
