# Generated by Containerization Assist using AI
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "photoalbum.fullname" . }}-test-connection"
  labels:
    {{- include "photoalbum.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "photoalbum.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
