# Generated by Containerization Assist using AI
# NOTE: In production, use Azure Key Vault with CSI driver or external-secrets operator
# instead of storing secrets directly in manifests.
# This template is provided as a reference for the expected secret structure.
apiVersion: v1
kind: Secret
metadata:
  name: photoalbum-secrets
  labels:
    {{- include "photoalbum.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Replace with your actual SQL Server connection string
  db-connection-string: "Server=tcp:<your-sql-server>.database.windows.net,1433;Database=PhotoAlbumDb;Authentication=Active Directory Default;"
